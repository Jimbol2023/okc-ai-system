import { createR92RevenueRiskEarlyWarningScopeContract, summarizeR92RevenueRiskEarlyWarningScope } from "./r92-revenue-risk-early-warning-scope-contract";
const reviewedInput = { revenueRiskEarlyWarningReviewed: true, pipelineFragilityReviewed: true, manualRiskReviewReviewed: true, riskAwarenessReviewed: true, controlledWarningVisibilityReviewed: true, riskWarningDoesNotExecuteReviewed: true, riskWarningDoesNotContactReviewed: true, riskWarningDoesNotCreateLeadsReviewed: true, noProviderReviewed: true, noRuntimeReviewed: true, noPersistenceReviewed: true, noAuditWritingReviewed: true, accessibilityReviewed: true, deterministicInvariantsReviewed: true, failClosedReviewed: true } as const;
describe("R92A revenue risk early warning scope contract", () => { it("defaults to operator review required", () => { const result = createR92RevenueRiskEarlyWarningScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("revenue risk early warning doctrine"); }); it("smoke-tests advisory-only early warning readiness", () => { const result = createR92RevenueRiskEarlyWarningScopeContract(reviewedInput); expect(result.status).toBe("revenue_risk_early_warning_scope_ready"); expect(result.flags.readOnly).toBe(true); expect(result.flags.advisoryOnly).toBe(true); expect(result.flags.simulationOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); }); it("defines deterministic advisory warning categories", () => { const result = createR92RevenueRiskEarlyWarningScopeContract(reviewedInput); expect(result.advisoryWarningCategories).toEqual(expect.arrayContaining(["early-risk-review-needed", "revenue-warning-signal", "pipeline-fragility-warning", "manual-risk-review-required", "operator-risk-coordination-needed", "advisory-warning-visibility-only"])); }); it("pressure-tests forbidden warning, provider, sourcing, runtime, and execution paths", () => { const result = createR92RevenueRiskEarlyWarningScopeContract({ ...reviewedInput, executionRequested: true, providerRequested: true, outreachRequested: true, runtimeRequested: true, automationRequested: true, leadGenerationRequested: true, scrapingRequested: true, skipTracingRequested: true, mlsPublicRecordRequested: true, fetchNetworkRequested: true, processEnvRequested: true, prismaWriteRequested: true, dbWriteRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_risk_early_warning_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["revenue risk early warning cannot execute", "provider calls remain blocked", "fetch/network remains blocked", "process.env remains blocked"])); }); it("preserves accessibility and governance boundaries", () => { const result = createR92RevenueRiskEarlyWarningScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.governanceBoundary.warningVisibilityOnlyMeans).toContain("execution remains blocked"); }); it("summarizes revenue risk early warning scope", () => { const result = createR92RevenueRiskEarlyWarningScopeContract(reviewedInput); expect(summarizeR92RevenueRiskEarlyWarningScope(result)).toMatch(/manual-review-only/i); expect(summarizeR92RevenueRiskEarlyWarningScope(result)).toMatch(/execution remain blocked/i); }); });
