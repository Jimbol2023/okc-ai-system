import { createR94RevenueRiskPrioritizationSafetyAccessibilityReview, summarizeR94RevenueRiskPrioritizationSafetyReview } from "./r94-revenue-risk-prioritization-safety-accessibility-review";
const reviewedInput = { riskPrioritizationExecutionReviewed: true, riskRankingAutomationReviewed: true, prioritySignalRuntimeReviewed: true, highPriorityProviderReviewed: true, prioritySignalOutreachReviewed: true, escalationPriorityContactReviewed: true, pipelineFragilityPriorityProviderReviewed: true, missingDataSkipTracingReviewed: true, priorityRiskPathScrapingReviewed: true, highImpactLeadCreationReviewed: true, providerPersistenceRuntimeReviewed: true, auditWritingReviewed: true, externalApiFetchEnvReviewed: true, accessibilityReviewed: true, governanceWarningsReviewed: true } as const;
describe("R94E revenue risk prioritization safety accessibility review", () => { it("defaults to operator review required", () => { const result = createR94RevenueRiskPrioritizationSafetyAccessibilityReview(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("risk prioritization visibility does not imply execution"); }); it("clears deterministic safety review when all areas are reviewed", () => { const result = createR94RevenueRiskPrioritizationSafetyAccessibilityReview(reviewedInput); expect(result.status).toBe("revenue_risk_prioritization_safety_clear"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); }); it("blocks execution, provider, outreach, sourcing, persistence, and audit drift", () => { const result = createR94RevenueRiskPrioritizationSafetyAccessibilityReview({ ...reviewedInput, executionRequested: true, automationRequested: true, runtimeRequested: true, providerRequested: true, outreachRequested: true, contactRequested: true, skipTracingRequested: true, scrapingRequested: true, leadCreationRequested: true, persistenceRequested: true, pollingRequested: true, auditWritingRequested: true, externalApiRequested: true, fetchNetworkRequested: true, processEnvRequested: true }); expect(result.status).toBe("revenue_risk_prioritization_safety_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["risk prioritization visibility cannot execute", "risk ranking cannot trigger automation", "high-priority risks and pipeline fragility priority cannot activate providers", "fetch/network remains blocked"])); }); it("preserves inclusive accessibility findings", () => { const result = createR94RevenueRiskPrioritizationSafetyAccessibilityReview(reviewedInput); expect(result.accessibility.semanticHeadingsPreserved).toBe(true); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes safety boundaries", () => { const result = createR94RevenueRiskPrioritizationSafetyAccessibilityReview(reviewedInput); expect(summarizeR94RevenueRiskPrioritizationSafetyReview(result)).toMatch(/risk-prioritization-does-not-execute/i); }); });
