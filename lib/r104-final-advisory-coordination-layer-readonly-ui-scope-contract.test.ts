import { createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract, summarizeR104FinalAdvisoryCoordinationLayerReadonlyUiScope } from "./r104-final-advisory-coordination-layer-readonly-ui-scope-contract";
const reviewedInput = { authorizedSurfacesReviewed: true, finalAdvisoryCoordinationWordingReviewed: true, finalAdvisoryCoordinationLayerReviewWordingReviewed: true, operatorCoordinationWordingReviewed: true, operatorReviewedFinalAdvisoryCoordinationWordingReviewed: true, highImpactCoordinationWordingReviewed: true, escalationCoordinationWordingReviewed: true, governanceFinalAdvisoryCoordinationReviewWordingReviewed: true, noExecutionWordingReviewed: true, coordinationDoesNotGrantExecutionWordingReviewed: true, noProviderWordingReviewed: true, noContactWordingReviewed: true, forbiddenControlsReviewed: true, accessibilityReviewed: true } as const;
describe("R104C Final advisory coordination layer read-only UI scope contract", () => { it("defaults to operator review required", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("authorized read-only surfaces"); }); it("authorizes only read-only surfaces", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract(reviewedInput); expect(result.status).toBe("final_advisory_coordination_layer_ui_scope_ready"); expect(result.authorizedSurfaces).toContain("components/dashboard/final-advisory-coordination-layer-summary.tsx"); }); it("keeps wording advisory, manual, and non-contacting", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract(reviewedInput); expect(result.wording.finalAdvisoryCoordination).toMatch(/Advisory final advisory coordination visibility only/i); expect(result.wording.finalAdvisoryCoordinationLayerReview).toMatch(/Final advisory coordination layer review recommended/i); expect(result.wording.noExecution).toMatch(/No execution/i); expect(result.wording.coordinationDoesNotGrantExecution).toMatch(/does not authorize execution/i); expect(result.wording.noProvider).toMatch(/No provider/i); }); it("blocks forbidden UI controls and behavior", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract({ ...reviewedInput, buttonRequested: true, formRequested: true, inputRequested: true, linkRequested: true, clickHandlerRequested: true, providerControlRequested: true, coordinationExecutionControlRequested: true, executionControlRequested: true, fetchNetworkRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("final_advisory_coordination_layer_ui_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["buttons remain forbidden", "links remain forbidden", "provider controls remain forbidden", "fetch/network remains blocked"])); }); it("preserves accessible read-only UI requirements", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes UI scope", () => { const result = createR104FinalAdvisoryCoordinationLayerReadonlyUiScopeContract(reviewedInput); expect(summarizeR104FinalAdvisoryCoordinationLayerReadonlyUiScope(result)).toMatch(/Final advisory coordination, Final advisory coordination layer review/i); }); });
