import { createR102ManualOperatorDecisionRoutingFinalLockdownContract, summarizeR102ManualOperatorDecisionRoutingFinalLockdown } from "./r102-manual-operator-decision-routing-final-lockdown-contract";
const reviewedInput = { r102aReviewed: true, r102bReviewed: true, r102cReviewed: true, r102dReviewed: true, r102eReviewed: true, lockdownRulesReviewed: true, accessibilityReviewed: true, operatorReviewCompleted: true } as const;
describe("R102F Manual operator decision routing final lockdown contract", () => { it("defaults to operator review required", () => { const result = createR102ManualOperatorDecisionRoutingFinalLockdownContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("R102A scope contract reviewed"); }); it("enforces final lockdown after all reviews", () => { const result = createR102ManualOperatorDecisionRoutingFinalLockdownContract(reviewedInput); expect(result.status).toBe("manual_operator_decision_routing_lockdown_enforced"); expect(result.flags.operatorDecisionRoutingLockdownEnforced).toBe(true); expect(result.nextPhase).toBe("R103 - Revenue Intelligence Consolidation"); }); it("locks every execution, provider, outreach, sourcing, runtime, persistence, and audit path", () => { const result = createR102ManualOperatorDecisionRoutingFinalLockdownContract({ ...reviewedInput, executionRequested: true, routingWorkRequested: true, approvalExecutionRequested: true, routingAsExecutionRequested: true, operatorDecisionRoutingReviewAutomationRequested: true, routingSignalRuntimeRequested: true, providerActivationRequested: true, outreachRequested: true, contactRequested: true, skipTracingRequested: true, scrapingRequested: true, leadCreationRequested: true, externalApiRequested: true, fetchNetworkRequested: true, pollingRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("manual_operator_decision_routing_lockdown_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["Manual operator decision routing intelligence never executes", "routing visibility never grants execution", "routing review never activates automation", "routing signals never activate runtime jobs", "fetch/network behavior remains blocked"])); }); it("declares explicit final rules", () => { const result = createR102ManualOperatorDecisionRoutingFinalLockdownContract(reviewedInput); expect(result.rules).toEqual(expect.arrayContaining(["Manual operator decision routing intelligence never executes.", "routing visibility never grants execution.", "operator decision routing visibility support never triggers outreach.", "Execution remains blocked."])); }); it("summarizes the lockdown", () => { const result = createR102ManualOperatorDecisionRoutingFinalLockdownContract(reviewedInput); expect(summarizeR102ManualOperatorDecisionRoutingFinalLockdown(result)).toMatch(/execution remains blocked/i); }); });
