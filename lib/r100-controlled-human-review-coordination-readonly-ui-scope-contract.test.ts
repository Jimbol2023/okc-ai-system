import { createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract, summarizeR100ControlledHumanReviewCoordinationReadonlyUiScope } from "./r100-controlled-human-review-coordination-readonly-ui-scope-contract";
const reviewedInput = { authorizedSurfacesReviewed: true, humanReviewCoordinationWordingReviewed: true, manualHumanReviewCoordinationReviewWordingReviewed: true, operatorCoordinationWordingReviewed: true, operatorReviewedHumanReviewCoordinationWordingReviewed: true, highImpactReviewCoordinationWordingReviewed: true, escalationReviewCoordinationWordingReviewed: true, governanceHumanReviewCoordinationReviewWordingReviewed: true, noExecutionWordingReviewed: true, coordinationDoesNotGrantExecutionWordingReviewed: true, noProviderWordingReviewed: true, noContactWordingReviewed: true, forbiddenControlsReviewed: true, accessibilityReviewed: true } as const;
describe("R100C controlled human review coordination read-only UI scope contract", () => { it("defaults to operator review required", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("authorized read-only surfaces"); }); it("authorizes only read-only surfaces", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract(reviewedInput); expect(result.status).toBe("controlled_human_review_coordination_ui_scope_ready"); expect(result.authorizedSurfaces).toContain("components/dashboard/controlled-human-review-coordination-summary.tsx"); }); it("keeps wording advisory, manual, and non-contacting", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract(reviewedInput); expect(result.wording.humanReviewCoordination).toMatch(/advisory review coordination visibility only/i); expect(result.wording.manualHumanReviewCoordinationReview).toMatch(/manual human review coordination review recommended/i); expect(result.wording.noExecution).toMatch(/No execution/i); expect(result.wording.coordinationDoesNotGrantExecution).toMatch(/does not authorize execution/i); expect(result.wording.noProvider).toMatch(/No provider/i); }); it("blocks forbidden UI controls and behavior", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract({ ...reviewedInput, buttonRequested: true, formRequested: true, inputRequested: true, linkRequested: true, clickHandlerRequested: true, providerControlRequested: true, coordinationExecutionControlRequested: true, executionControlRequested: true, fetchNetworkRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("controlled_human_review_coordination_ui_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["buttons remain forbidden", "links remain forbidden", "provider controls remain forbidden", "fetch/network remains blocked"])); }); it("preserves accessible read-only UI requirements", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes UI scope", () => { const result = createR100ControlledHumanReviewCoordinationReadonlyUiScopeContract(reviewedInput); expect(summarizeR100ControlledHumanReviewCoordinationReadonlyUiScope(result)).toMatch(/human review coordination, manual human review coordination review/i); }); });
