import { createR102ManualOperatorDecisionRoutingScopeContract, summarizeR102ManualOperatorDecisionRoutingScope } from "./r102-manual-operator-decision-routing-scope-contract";
const reviewedInput = { manualOperatorDecisionRoutingReviewed: true, manualRoutingReviewPrepared: true, governanceFirstOperatorDecisionRoutingReviewed: true, operatorReviewedOperatorDecisionRoutingReviewed: true, operatorDecisionRoutingDoesNotExecuteReviewed: true, operatorDecisionRoutingDoesNotRouteWorkReviewed: true, operatorDecisionRoutingDoesNotGrantApprovalReviewed: true, operatorDecisionRoutingDoesNotGrantExecutionReviewed: true, operatorDecisionRoutingDoesNotContactReviewed: true, operatorDecisionRoutingDoesNotCreateLeadsReviewed: true, noProviderReviewed: true, noRuntimeReviewed: true, noPersistenceReviewed: true, noAuditWritingReviewed: true, accessibilityReviewed: true, deterministicInvariantsReviewed: true, failClosedReviewed: true } as const;
describe("R102A Manual operator decision routing scope contract", () => { it("defaults to operator review required", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("Manual operator decision routing doctrine"); }); it("smoke-tests advisory-only operator decision routing review readiness", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract(reviewedInput); expect(result.status).toBe("manual_operator_decision_routing_scope_ready"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); }); it("defines deterministic advisory command categories", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract(reviewedInput); expect(result.advisoryOperatorDecisionRoutingCategories).toEqual(expect.arrayContaining(["manual-routing-review-needed", "operator-decision-routing-review-required", "command-level-routing-summary", "advisory-routing-visibility-only"])); }); it("pressure-tests forbidden routing risk paths", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract({ ...reviewedInput, executionRequested: true, routingWorkRequested: true, approvalExecutionRequested: true, routingAsExecutionRequested: true, providerRequested: true, outreachRequested: true, runtimeRequested: true, automationRequested: true, leadGenerationRequested: true, scrapingRequested: true, skipTracingRequested: true, mlsPublicRecordRequested: true, fetchNetworkRequested: true, processEnvRequested: true, prismaWriteRequested: true, dbWriteRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("manual_operator_decision_routing_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["Manual operator decision routing cannot execute", "routing visibility does not grant execution", "fetch/network remains blocked", "process.env remains blocked"])); }); it("preserves accessibility and governance boundaries", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract(reviewedInput); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.governanceBoundary.operatorDecisionRoutingVisibilityOnlyMeans).toContain("execution remains blocked"); }); it("summarizes Manual operator decision routing scope", () => { const result = createR102ManualOperatorDecisionRoutingScopeContract(reviewedInput); expect(summarizeR102ManualOperatorDecisionRoutingScope(result)).toMatch(/manual-review-only/i); }); });
