import { createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview, summarizeR102ManualOperatorDecisionRoutingSafetyReview } from "./r102-manual-operator-decision-routing-safety-accessibility-review";
const reviewedInput = { operatorDecisionRoutingExecutionReviewed: true, operatorDecisionRoutingDoesNotRouteWorkReviewed: true, operatorDecisionRoutingDoesNotGrantApprovalReviewed: true, operatorDecisionRoutingDoesNotGrantExecutionReviewed: true, operatorDecisionRoutingReviewAutomationReviewed: true, routingSignalRuntimeReviewed: true, highImpactRoutingProviderReviewed: true, routingSignalOutreachReviewed: true, escalationRoutingContactReviewed: true, riskPathRoutingProviderReviewed: true, missingDataSkipTracingReviewed: true, routingRiskPathScrapingReviewed: true, highImpactRoutingLeadCreationReviewed: true, providerPersistenceRuntimeReviewed: true, auditWritingReviewed: true, externalApiFetchEnvReviewed: true, accessibilityReviewed: true, governanceWarningsReviewed: true } as const;
describe("R102E Manual operator decision routing safety accessibility review", () => { it("defaults to operator review required", () => { const result = createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("operator decision routing visibility does not imply execution"); }); it("clears deterministic safety review when all areas are reviewed", () => { const result = createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview(reviewedInput); expect(result.status).toBe("manual_operator_decision_routing_safety_clear"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); }); it("blocks execution, provider, outreach, sourcing, persistence, and audit drift", () => { const result = createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview({ ...reviewedInput, executionRequested: true, automationRequested: true, runtimeRequested: true, routingWorkRequested: true, approvalExecutionRequested: true, routingAsExecutionRequested: true, providerRequested: true, outreachRequested: true, contactRequested: true, skipTracingRequested: true, scrapingRequested: true, leadCreationRequested: true, persistenceRequested: true, pollingRequested: true, auditWritingRequested: true, externalApiRequested: true, fetchNetworkRequested: true, processEnvRequested: true }); expect(result.status).toBe("manual_operator_decision_routing_safety_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["operator decision routing visibility cannot execute", "routing visibility cannot grant execution", "routing review visibility cannot trigger automation", "high-impact routing and risk-path routing cannot activate providers", "fetch/network remains blocked"])); }); it("preserves inclusive accessibility findings", () => { const result = createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview(reviewedInput); expect(result.accessibility.semanticHeadingsPreserved).toBe(true); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes safety boundaries", () => { const result = createR102ManualOperatorDecisionRoutingSafetyAccessibilityReview(reviewedInput); expect(summarizeR102ManualOperatorDecisionRoutingSafetyReview(result)).toMatch(/operator-decision-routing-does-not-execute/i); }); });
