import { createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract, summarizeR102ManualOperatorDecisionRoutingReadonlyUiScope } from "./r102-manual-operator-decision-routing-readonly-ui-scope-contract";
const reviewedInput = { authorizedSurfacesReviewed: true, operatorDecisionRoutingWordingReviewed: true, manualOperatorDecisionRoutingReviewWordingReviewed: true, operatorRoutingWordingReviewed: true, operatorReviewedOperatorDecisionRoutingWordingReviewed: true, highImpactRoutingWordingReviewed: true, escalationRoutingWordingReviewed: true, governanceOperatorDecisionRoutingReviewWordingReviewed: true, noExecutionWordingReviewed: true, routingDoesNotGrantExecutionWordingReviewed: true, noProviderWordingReviewed: true, noContactWordingReviewed: true, forbiddenControlsReviewed: true, accessibilityReviewed: true } as const;
describe("R102C Manual operator decision routing read-only UI scope contract", () => { it("defaults to operator review required", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("authorized read-only surfaces"); }); it("authorizes only read-only surfaces", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract(reviewedInput); expect(result.status).toBe("manual_operator_decision_routing_ui_scope_ready"); expect(result.authorizedSurfaces).toContain("components/dashboard/manual-operator-decision-routing-summary.tsx"); }); it("keeps wording advisory, manual, and non-contacting", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract(reviewedInput); expect(result.wording.operatorDecisionRouting).toMatch(/advisory routing visibility only/i); expect(result.wording.manualOperatorDecisionRoutingReview).toMatch(/Manual operator decision routing review recommended/i); expect(result.wording.noExecution).toMatch(/No execution/i); expect(result.wording.routingDoesNotGrantExecution).toMatch(/does not authorize execution/i); expect(result.wording.noProvider).toMatch(/No provider/i); }); it("blocks forbidden UI controls and behavior", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract({ ...reviewedInput, buttonRequested: true, formRequested: true, inputRequested: true, linkRequested: true, clickHandlerRequested: true, providerControlRequested: true, routingExecutionControlRequested: true, executionControlRequested: true, fetchNetworkRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("manual_operator_decision_routing_ui_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["buttons remain forbidden", "links remain forbidden", "provider controls remain forbidden", "fetch/network remains blocked"])); }); it("preserves accessible read-only UI requirements", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes UI scope", () => { const result = createR102ManualOperatorDecisionRoutingReadonlyUiScopeContract(reviewedInput); expect(summarizeR102ManualOperatorDecisionRoutingReadonlyUiScope(result)).toMatch(/operator decision routing, Manual operator decision routing review/i); }); });
