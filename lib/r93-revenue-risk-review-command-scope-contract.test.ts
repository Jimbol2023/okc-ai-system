import { createR93RevenueRiskReviewCommandScopeContract, summarizeR93RevenueRiskReviewCommandScope } from "./r93-revenue-risk-review-command-scope-contract";
const reviewedInput = { revenueRiskReviewCommandReviewed: true, manualRiskReviewReviewed: true, operatorRiskCoordinationReviewed: true, earlyWarningReviewReadinessReviewed: true, riskCommandVisibilityReviewed: true, riskReviewCommandDoesNotExecuteReviewed: true, riskReviewCommandDoesNotContactReviewed: true, riskReviewCommandDoesNotCreateLeadsReviewed: true, noProviderReviewed: true, noRuntimeReviewed: true, noPersistenceReviewed: true, noAuditWritingReviewed: true, accessibilityReviewed: true, deterministicInvariantsReviewed: true, failClosedReviewed: true } as const;
describe("R93A revenue risk review command scope contract", () => { it("defaults to operator review required", () => { const result = createR93RevenueRiskReviewCommandScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("revenue risk review command doctrine"); }); it("smoke-tests advisory-only command readiness", () => { const result = createR93RevenueRiskReviewCommandScopeContract(reviewedInput); expect(result.status).toBe("revenue_risk_review_command_scope_ready"); expect(result.flags.readOnly).toBe(true); expect(result.flags.advisoryOnly).toBe(true); expect(result.flags.simulationOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); }); it("defines deterministic advisory command categories", () => { const result = createR93RevenueRiskReviewCommandScopeContract(reviewedInput); expect(result.advisoryRiskCommandCategories).toEqual(expect.arrayContaining(["risk-review-command-needed", "manual-risk-review-required", "operator-risk-coordination-needed", "command-level-risk-summary", "advisory-risk-command-visibility-only"])); }); it("pressure-tests forbidden command, provider, sourcing, runtime, and execution paths", () => { const result = createR93RevenueRiskReviewCommandScopeContract({ ...reviewedInput, executionRequested: true, providerRequested: true, outreachRequested: true, runtimeRequested: true, automationRequested: true, leadGenerationRequested: true, scrapingRequested: true, skipTracingRequested: true, mlsPublicRecordRequested: true, fetchNetworkRequested: true, processEnvRequested: true, prismaWriteRequested: true, dbWriteRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_risk_review_command_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["revenue risk review command cannot execute", "provider calls remain blocked", "fetch/network remains blocked", "process.env remains blocked"])); }); it("preserves accessibility and governance boundaries", () => { const result = createR93RevenueRiskReviewCommandScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.governanceBoundary.commandVisibilityOnlyMeans).toContain("execution remains blocked"); }); it("summarizes revenue risk review command scope", () => { const result = createR93RevenueRiskReviewCommandScopeContract(reviewedInput); expect(summarizeR93RevenueRiskReviewCommandScope(result)).toMatch(/manual-review-only/i); expect(summarizeR93RevenueRiskReviewCommandScope(result)).toMatch(/execution remain blocked/i); }); });
