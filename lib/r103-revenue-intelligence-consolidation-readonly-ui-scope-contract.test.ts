import { createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract, summarizeR103RevenueIntelligenceConsolidationReadonlyUiScope } from "./r103-revenue-intelligence-consolidation-readonly-ui-scope-contract";
const reviewedInput = { authorizedSurfacesReviewed: true, revenueIntelligenceWordingReviewed: true, revenueIntelligenceConsolidationReviewWordingReviewed: true, operatorIntelligenceWordingReviewed: true, operatorReviewedRevenueIntelligenceWordingReviewed: true, highImpactIntelligenceWordingReviewed: true, escalationIntelligenceWordingReviewed: true, governanceRevenueIntelligenceReviewWordingReviewed: true, noExecutionWordingReviewed: true, intelligenceDoesNotGrantExecutionWordingReviewed: true, noProviderWordingReviewed: true, noContactWordingReviewed: true, forbiddenControlsReviewed: true, accessibilityReviewed: true } as const;
describe("R103C Revenue intelligence consolidation read-only UI scope contract", () => { it("defaults to operator review required", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("authorized read-only surfaces"); }); it("authorizes only read-only surfaces", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract(reviewedInput); expect(result.status).toBe("revenue_intelligence_consolidation_ui_scope_ready"); expect(result.authorizedSurfaces).toContain("components/dashboard/revenue-intelligence-consolidation-summary.tsx"); }); it("keeps wording advisory, manual, and non-contacting", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract(reviewedInput); expect(result.wording.revenueIntelligence).toMatch(/advisory revenue intelligence visibility only/i); expect(result.wording.revenueIntelligenceConsolidationReview).toMatch(/Revenue intelligence consolidation review recommended/i); expect(result.wording.noExecution).toMatch(/No execution/i); expect(result.wording.intelligenceDoesNotGrantExecution).toMatch(/does not authorize execution/i); expect(result.wording.noProvider).toMatch(/No provider/i); }); it("blocks forbidden UI controls and behavior", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract({ ...reviewedInput, buttonRequested: true, formRequested: true, inputRequested: true, linkRequested: true, clickHandlerRequested: true, providerControlRequested: true, intelligenceExecutionControlRequested: true, executionControlRequested: true, fetchNetworkRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_intelligence_consolidation_ui_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["buttons remain forbidden", "links remain forbidden", "provider controls remain forbidden", "fetch/network remains blocked"])); }); it("preserves accessible read-only UI requirements", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes UI scope", () => { const result = createR103RevenueIntelligenceConsolidationReadonlyUiScopeContract(reviewedInput); expect(summarizeR103RevenueIntelligenceConsolidationReadonlyUiScope(result)).toMatch(/revenue intelligence, Revenue intelligence consolidation review/i); }); });
