import { createR94RevenueRiskPrioritizationReadonlyUiScopeContract, summarizeR94RevenueRiskPrioritizationReadonlyUiScope } from "./r94-revenue-risk-prioritization-readonly-ui-scope-contract";
const reviewedInput = { authorizedSurfacesReviewed: true, riskPrioritizationWordingReviewed: true, manualRiskRankingWordingReviewed: true, operatorRiskTriageWordingReviewed: true, commandLevelPriorityWordingReviewed: true, revenueContinuityPriorityWordingReviewed: true, pipelineFragilityPriorityWordingReviewed: true, escalationPriorityWordingReviewed: true, governancePriorityWordingReviewed: true, noExecutionWordingReviewed: true, noProviderWordingReviewed: true, noContactWordingReviewed: true, forbiddenControlsReviewed: true, accessibilityReviewed: true } as const;
describe("R94C revenue risk prioritization read-only UI scope contract", () => { it("defaults to operator review required", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("authorized read-only surfaces"); }); it("authorizes only read-only surfaces", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract(reviewedInput); expect(result.status).toBe("revenue_risk_prioritization_ui_scope_ready"); expect(result.authorizedSurfaces).toContain("components/dashboard/revenue-risk-prioritization-summary.tsx"); }); it("keeps wording advisory, manual, and non-contacting", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract(reviewedInput); expect(result.wording.riskPrioritization).toMatch(/advisory priority visibility only/i); expect(result.wording.manualRiskRanking).toMatch(/Manual risk ranking review recommended/i); expect(result.wording.noExecution).toMatch(/No execution/i); expect(result.wording.noProvider).toMatch(/No provider/i); expect(result.wording.noContact).toMatch(/No buyer, seller/i); }); it("blocks forbidden UI controls and behavior", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract({ ...reviewedInput, buttonRequested: true, formRequested: true, inputRequested: true, linkRequested: true, clickHandlerRequested: true, providerControlRequested: true, executionControlRequested: true, fetchNetworkRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_risk_prioritization_ui_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["buttons remain forbidden", "links remain forbidden", "provider controls remain forbidden", "fetch/network remains blocked"])); }); it("preserves accessible read-only UI requirements", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract(reviewedInput); expect(result.accessibility.semanticHeadings).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); }); it("summarizes UI scope", () => { const result = createR94RevenueRiskPrioritizationReadonlyUiScopeContract(reviewedInput); expect(summarizeR94RevenueRiskPrioritizationReadonlyUiScope(result)).toMatch(/risk prioritization, manual risk ranking/i); }); });
