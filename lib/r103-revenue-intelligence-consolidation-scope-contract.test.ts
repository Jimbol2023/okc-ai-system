import { createR103RevenueIntelligenceConsolidationScopeContract, summarizeR103RevenueIntelligenceConsolidationScope } from "./r103-revenue-intelligence-consolidation-scope-contract";
const reviewedInput = { revenueIntelligenceConsolidationReviewed: true, manualIntelligenceReviewPrepared: true, governanceFirstRevenueIntelligenceReviewed: true, operatorReviewedRevenueIntelligenceReviewed: true, revenueIntelligenceDoesNotExecuteReviewed: true, revenueIntelligenceDoesNotRouteWorkReviewed: true, revenueIntelligenceDoesNotGrantApprovalReviewed: true, revenueIntelligenceDoesNotGrantExecutionReviewed: true, revenueIntelligenceDoesNotContactReviewed: true, revenueIntelligenceDoesNotCreateLeadsReviewed: true, noProviderReviewed: true, noRuntimeReviewed: true, noPersistenceReviewed: true, noAuditWritingReviewed: true, accessibilityReviewed: true, deterministicInvariantsReviewed: true, failClosedReviewed: true } as const;
describe("R103A Revenue intelligence consolidation scope contract", () => { it("defaults to operator review required", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("Revenue intelligence consolidation doctrine"); }); it("smoke-tests advisory-only revenue intelligence review readiness", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract(reviewedInput); expect(result.status).toBe("revenue_intelligence_consolidation_scope_ready"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); }); it("defines deterministic advisory command categories", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract(reviewedInput); expect(result.advisoryRevenueIntelligenceCategories).toEqual(expect.arrayContaining(["consolidated-revenue-intelligence-review-needed", "revenue-intelligence-review-required", "command-level-intelligence-summary", "advisory-intelligence-visibility-only"])); }); it("pressure-tests forbidden intelligence risk paths", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract({ ...reviewedInput, executionRequested: true, intelligenceWorkRequested: true, approvalExecutionRequested: true, intelligenceAsExecutionRequested: true, providerRequested: true, outreachRequested: true, runtimeRequested: true, automationRequested: true, leadGenerationRequested: true, scrapingRequested: true, skipTracingRequested: true, mlsPublicRecordRequested: true, fetchNetworkRequested: true, processEnvRequested: true, prismaWriteRequested: true, dbWriteRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_intelligence_consolidation_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["Revenue intelligence consolidation cannot execute", "revenue intelligence visibility does not grant execution", "fetch/network remains blocked", "process.env remains blocked"])); }); it("preserves accessibility and governance boundaries", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract(reviewedInput); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.governanceBoundary.revenueIntelligenceVisibilityOnlyMeans).toContain("execution remains blocked"); }); it("summarizes Revenue intelligence consolidation scope", () => { const result = createR103RevenueIntelligenceConsolidationScopeContract(reviewedInput); expect(summarizeR103RevenueIntelligenceConsolidationScope(result)).toMatch(/manual-review-only/i); }); });
