import { createR97RevenueRiskActionRecommendationScopeContract, summarizeR97RevenueRiskActionRecommendationScope } from "./r97-revenue-risk-action-recommendation-scope-contract";
const reviewedInput = { revenueRiskActionRecommendationReviewed: true, manualActionReviewReviewed: true, governanceFirstActionReviewReadinessReviewed: true, operatorReviewedActionReviewed: true, recommendationDoesNotExecuteReviewed: true, recommendationDoesNotContactReviewed: true, recommendationDoesNotCreateLeadsReviewed: true, noProviderReviewed: true, noRuntimeReviewed: true, noPersistenceReviewed: true, noAuditWritingReviewed: true, accessibilityReviewed: true, deterministicInvariantsReviewed: true, failClosedReviewed: true } as const;
describe("R97A revenue risk action recommendation scope contract", () => { it("defaults to operator review required", () => { const result = createR97RevenueRiskActionRecommendationScopeContract(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("revenue risk action recommendation doctrine"); }); it("smoke-tests advisory-only action review readiness", () => { const result = createR97RevenueRiskActionRecommendationScopeContract(reviewedInput); expect(result.status).toBe("revenue_risk_action_recommendation_scope_ready"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); }); it("defines deterministic advisory recommendation categories", () => { const result = createR97RevenueRiskActionRecommendationScopeContract(reviewedInput); expect(result.advisoryRecommendationCategories).toEqual(expect.arrayContaining(["action-review-needed", "manual-action-recommendation-required", "command-level-recommendation-summary", "advisory-recommendation-visibility-only"])); }); it("pressure-tests forbidden recommendation risk paths", () => { const result = createR97RevenueRiskActionRecommendationScopeContract({ ...reviewedInput, executionRequested: true, providerRequested: true, outreachRequested: true, runtimeRequested: true, automationRequested: true, leadGenerationRequested: true, scrapingRequested: true, skipTracingRequested: true, mlsPublicRecordRequested: true, fetchNetworkRequested: true, processEnvRequested: true, prismaWriteRequested: true, dbWriteRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_risk_action_recommendation_scope_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["revenue risk action recommendation cannot execute", "provider calls remain blocked", "fetch/network remains blocked", "process.env remains blocked"])); }); it("preserves accessibility and governance boundaries", () => { const result = createR97RevenueRiskActionRecommendationScopeContract(reviewedInput); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.governanceBoundary.recommendationVisibilityOnlyMeans).toContain("execution remains blocked"); }); it("summarizes revenue risk action recommendation scope", () => { const result = createR97RevenueRiskActionRecommendationScopeContract(reviewedInput); expect(summarizeR97RevenueRiskActionRecommendationScope(result)).toMatch(/manual-review-only/i); }); });
