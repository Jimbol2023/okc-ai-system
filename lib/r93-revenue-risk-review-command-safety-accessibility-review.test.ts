import { createR93RevenueRiskReviewCommandSafetyAccessibilityReview, summarizeR93RevenueRiskReviewCommandSafetyReview } from "./r93-revenue-risk-review-command-safety-accessibility-review";
const reviewedInput = { riskReviewCommandExecutionReviewed: true, manualRiskReviewAutomationReviewed: true, earlyWarningReviewRuntimeReviewed: true, riskCoordinationProviderReviewed: true, revenueContinuityRiskOutreachReviewed: true, escalationReviewContactReviewed: true, pipelineFragilityProviderReviewed: true, missingDataSkipTracingReviewed: true, riskReviewPathScrapingReviewed: true, highImpactLeadCreationReviewed: true, providerPersistenceRuntimeReviewed: true, auditWritingReviewed: true, externalApiFetchEnvReviewed: true, accessibilityReviewed: true, governanceWarningsReviewed: true } as const;
describe("R93E revenue risk review command safety accessibility review", () => { it("defaults to operator review required", () => { const result = createR93RevenueRiskReviewCommandSafetyAccessibilityReview(); expect(result.status).toBe("operator_review_required"); expect(result.missingReviewAreas).toContain("risk review command visibility does not imply execution"); }); it("clears deterministic safety review when all areas are reviewed", () => { const result = createR93RevenueRiskReviewCommandSafetyAccessibilityReview(reviewedInput); expect(result.status).toBe("revenue_risk_review_command_safety_clear"); expect(result.flags.readOnly).toBe(true); expect(result.flags.providerActivationAllowed).toBe(false); }); it("blocks execution, provider, outreach, sourcing, persistence, and audit drift", () => { const result = createR93RevenueRiskReviewCommandSafetyAccessibilityReview({ ...reviewedInput, executionRequested: true, automationRequested: true, runtimeRequested: true, providerRequested: true, outreachRequested: true, contactRequested: true, skipTracingRequested: true, scrapingRequested: true, leadCreationRequested: true, persistenceRequested: true, pollingRequested: true, auditWritingRequested: true, externalApiRequested: true, fetchNetworkRequested: true, processEnvRequested: true }); expect(result.status).toBe("revenue_risk_review_command_safety_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["risk review command visibility cannot execute", "manual risk review cannot trigger automation", "risk coordination and pipeline fragility review cannot activate providers", "fetch/network remains blocked"])); }); it("preserves inclusive accessibility findings", () => { const result = createR93RevenueRiskReviewCommandSafetyAccessibilityReview(reviewedInput); expect(result.accessibility.semanticHeadingsPreserved).toBe(true); expect(result.accessibility.noColorOnlyMeaning).toBe(true); expect(result.accessibility.noAutoRefresh).toBe(true); expect(result.findings).toContain("Semantic accessibility, readable labels, no color-only meaning, no motion dependency, no auto-refresh, and visible governance warnings are required."); }); it("summarizes safety boundaries", () => { const result = createR93RevenueRiskReviewCommandSafetyAccessibilityReview(reviewedInput); expect(summarizeR93RevenueRiskReviewCommandSafetyReview(result)).toMatch(/risk-review-command-does-not-execute/i); }); });
