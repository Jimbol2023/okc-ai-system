import { createR91RevenueResilienceStabilizationFinalLockdownContract, summarizeR91RevenueResilienceStabilizationFinalLockdown } from "./r91-revenue-resilience-stabilization-final-lockdown-contract";
const reviewedInput = { r91aReviewed: true, r91bReviewed: true, r91cReviewed: true, r91dReviewed: true, r91eReviewed: true, lockdownRulesReviewed: true, accessibilityReviewed: true, operatorReviewCompleted: true } as const;
describe("R91F revenue resilience stabilization final lockdown contract", () => { it("defaults to operator review required with lockdown enforced", () => { const result = createR91RevenueResilienceStabilizationFinalLockdownContract(); expect(result.status).toBe("operator_review_required"); expect(result.flags.revenueResilienceStabilizationLockdownEnforced).toBe(true); expect(result.rules).toContain("Revenue resilience intelligence never executes."); }); it("smoke-tests a fully reviewed lockdown", () => { const result = createR91RevenueResilienceStabilizationFinalLockdownContract(reviewedInput); expect(result.status).toBe("revenue_resilience_stabilization_lockdown_enforced"); expect(result.phase).toBe("R91F"); expect(result.nextPhase).toBe("R92 - Revenue Risk Early Warning Intelligence"); }); it("preserves all non-execution flags", () => { const result = createR91RevenueResilienceStabilizationFinalLockdownContract(reviewedInput); expect(result.flags.readOnly).toBe(true); expect(result.flags.advisoryOnly).toBe(true); expect(result.flags.simulationOnly).toBe(true); expect(result.flags.providerCalled).toBe(false); expect(result.flags.sent).toBe(false); expect(result.flags.persistenceAllowedNow).toBe(false); expect(result.flags.pollingAllowed).toBe(false); expect(result.flags.runtimeActivationAllowed).toBe(false); expect(result.flags.providerActivationAllowed).toBe(false); expect(result.flags.approvalGrantsExecution).toBe(false); expect(result.flags.executionAllowed).toBe(false); }); it("pressure-tests every forbidden request as blocked", () => { const result = createR91RevenueResilienceStabilizationFinalLockdownContract({ ...reviewedInput, executionRequested: true, stabilizationAutomationRequested: true, continuityRuntimeRequested: true, providerActivationRequested: true, outreachRequested: true, contactRequested: true, skipTracingRequested: true, scrapingRequested: true, leadCreationRequested: true, externalApiRequested: true, fetchNetworkRequested: true, pollingRequested: true, persistenceRequested: true, auditWritingRequested: true }); expect(result.status).toBe("revenue_resilience_stabilization_lockdown_blocked"); expect(result.blockedReasons).toEqual(expect.arrayContaining(["revenue resilience intelligence never executes", "stabilization guidance never activates automation", "revenue continuity signals never activate runtime jobs", "high-impact stability visibility never creates leads"])); }); it("summarizes the final lockdown", () => { const result = createR91RevenueResilienceStabilizationFinalLockdownContract(reviewedInput); expect(summarizeR91RevenueResilienceStabilizationFinalLockdown(result)).toMatch(/revenue resilience intelligence never executes/i); expect(summarizeR91RevenueResilienceStabilizationFinalLockdown(result)).toMatch(/revenue continuity signals never activate runtime jobs/i); expect(summarizeR91RevenueResilienceStabilizationFinalLockdown(result)).toMatch(/execution remains blocked/i); }); });
